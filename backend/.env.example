# Sunucu portu
PORT=4000

# Frontend'in çalıştığı adres (CORS için)
CORS_ORIGIN=http://localhost:5173

# İleride gerçek bir veritabanına geçildiğinde burada bağlantı bilgisi
# tutulur (ör. DATABASE_URL=postgres://...). Şimdilik kullanılmıyor —
# veriler backend/src/data/*.json dosyalarında tutuluyor.
